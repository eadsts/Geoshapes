package com.maxtrain.geoshapesproject;

public interface Iperimeter {
	//any class that implements this interface must have a method with the same parameters
	//and returns the same type
	int perimeter();
}
